<div class="aside">
    <h3 class="aside-title">Фильтр</h3>
</div>
@foreach (var f in Model.Item1)
{
    <div class="aside">
        @await Html.PartialAsync($"PartialView/Filter/FiltrationTypes/{f.Value.filtrationType.GetType().Name}", new KeyValuePair<int, FilterElement>(f.Key, f.Value))
    </div>
}
<div class="aside">
    <input type='submit' class='filter-installation-btn' value='Применить'/>
</div>
<div class="aside-10px">
    <a href="/Shop/Section/@Model.Item2" class='reset-filter-btn'>Сбросить</a>
</div>