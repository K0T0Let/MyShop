<div class="aside">
    <h3 class="aside-title">Фильтр</h3>
</div>
@foreach (var c in Model)
{
    <div class="aside">
        @await Html.PartialAsync($"PartialView/Filter/FiltrationTypes/{c.Value.filtrationType.GetType().Name}", new KeyValuePair<string, Characteristic>(c.Key, c.Value))
    </div>
}
<div class="aside">
	<input type='submit' class='primary-btn' value='Найти'/>
</div>